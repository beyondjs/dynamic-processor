# Main module

The public `@beyond-js/dynamic-processor/main` module combines the mixin factory, lifecycle implementation and child-monitor collaborators. Internal folders do not define separate public module identities.

Read [architecture and lifecycle](../../../docs/architecture.md) and [mixin forwarding](overloads.md) before extending hooks or changing notification timing.
