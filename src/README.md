# Dynamic Processor source package

This Beyond package contains the public `main` module and its internal lifecycle, child-monitor and logging objects. Read the repository [overview](../README.md) and [architecture](../docs/architecture.md) for usage, build prerequisites and limitations.
